# <你的大脑名> — AI 行为路由（真相源宪法）

> 本文件是 AI 操作本知识库的行为入口。**每个 AI 进来第一动作 = 读这份文件。**
> 改编自 Karpathy LLM Wiki 模式 + 本地专家大脑绿皮书三层架构。

## 0. 这是什么

这是一个"本地专家大脑/第二大脑"的真相源（Single Source of Truth）。
原则：**代谢 > 囤积**——知识要被消化成结构、被精准检索、被综合，而非囤着。

## 1. 三层架构（硬约束）

| 层 | 目录 | 内容 | AI 权限 |
|---|---|---|---|
| Layer 1 原料 | `sources/` | 文章/纪要/转写/录音稿/想法（原始） | **只读不可变**，绝不改原文 |
| Layer 2 知识 | `concepts/` `syntheses/` `entities/` | AI 编译的结构化知识 | 创建 + 维护 |
| Layer 3 洞察 | `insights/` | 人类手写的主观判断/复盘/立场 | **永不触碰**，只读引用 |

要基于源材料产出知识，写入 Layer 2，**绝不修改 Layer 1 原文**。

## 2. 三个操作

- **Ingest（摄入消化）**：人把材料扔进 `sources/`，说"帮我录入"→ AI 读完，在 Layer 2 创建/更新 5-15 个知识页（概念页、实体页、综述页），加交叉引用，更新 `index.md`，追加 `log.md`。
- **Query（查询）**：基于已有知识页回答问题，带 `[[wikilink]]` 引用；若产生跨源综合/新对比/可行动洞察，自动回写为新页（write-back）。
- **Lint（健康检查）**：定期查孤页/断链/矛盾/过期/缺失概念。

## 3. 关键文件

- `index.md` — 手写入口页，知识地图（AI 每次 query 第一站）
- `log.md` — append-only 操作日志，格式：`## [YYYY-MM-DD] 操作 | 标题`
- 本文件（CLAUDE.md / 或 _schema.md）— 规则本身

## 4. 命名与规范

- 概念/实体页：小写连字符 ASCII（`transformer-architecture.md`），中文概念用拼音 slug
- 源文件：保留原名或 `YYYY-MM-DD-slug.md`，中文文件名可保留以存溯源
- 新页至少 3 个出站 `[[wikilink]]`
- 创建新页前**先搜是否已有同概念页**，避免重复（复用 > 重写）

## 5. 检索（接入 QMD 后）

知识库长大后（文件近千 / 概念数百），用 QMD 做全文混合检索：
- 找细节/跨类语义：`qmd query "问题"`
- 找确切词：`qmd search "词"`
- QMD 覆盖**全 vault 含未编译的 raw**，补 wiki 页只存骨架的缺口。

## 6. 数据可靠性（重要）

- `sources/` 里的纪要/转写**数字默认存疑**（实测会被放大），重要数字须交叉验证后才信。
- 若有人工校正的事实，单独放 `facts/` 标最高信任。

## 7. PII / 敏感数据

- 敏感数据（客户/个人隐私）放专门子树，整子树写进 `.gitignore`（**本地可问答、git 不带走**）。
- 给他人看走脱敏，原始 PII 永留本地。
